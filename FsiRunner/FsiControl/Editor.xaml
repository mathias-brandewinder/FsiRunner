<UserControl x:Class="ClearLines.FsiControl.Editor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DockPanel>
        <Border DockPanel.Dock="Top">
            <StackPanel>
                <ListView ItemsSource="{Binding CodeBlocks}" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBox  
                                    Text="{Binding Code}"                  
                                    AcceptsReturn="True"
                                    TextWrapping="Wrap"
                                    Background="LightGray" 
                                    FontFamily="Consolas" 
                                    FontSize="13" HorizontalScrollBarVisibility="Disabled"/>
                                <Border>
                                    <Button
                                        Command="{Binding Run}"
                                        HorizontalAlignment="Right"
                                        Content="Run" 
                                        FontSize="9"
                                        Width="30" 
                                        Height="20"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Border>
                    <Button
                        Command="{Binding AddCodeBlock}"
                        Content="+"
                        HorizontalAlignment="Right"
                        FontSize="12"/>
                </Border>
            </StackPanel>
        </Border>
        <ListView ItemsSource="{Binding FeedbackBlocks}" 
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            HorizontalContentAlignment="Stretch"
            Margin="0,5,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock  
                        Text="{Binding Result}"
                        TextWrapping="Wrap"
                        FontFamily="Consolas"
                        Background="LightGray"
                        FontSize="13"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>