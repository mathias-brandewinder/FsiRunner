<UserControl x:Class="ClearLines.FsiControl.Editor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:FsiControl="clr-namespace:ClearLines.FsiControl" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <FsiControl:EditorViewModel x:Key="ViewModel"/>
    </UserControl.Resources>
    <!--<UserControl DataContext="{Binding Source={StaticResource ViewModel}}"/>-->

    <DockPanel DataContext="{StaticResource ViewModel}">
        <DockPanel DockPanel.Dock="Top">
            <Button
                Command="{Binding AddCodeBlock}"
                DockPanel.Dock="Bottom"
                Content="New Block"
                HorizontalAlignment="Right"
                Width="70" Height="28"/>
            <ListView ItemsSource="{Binding CodeBlocks}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <DockPanel >
                            <Button 
                                DockPanel.Dock="Right" 
                                Command="{Binding Run}"
                                Content="Run" FontSize="9"
                                Width="30" Height="20"/>
                            <TextBox 
                                Width="200"
                                Text="{Binding Code}"                  
                                TextWrapping="Wrap" 
                                AcceptsReturn="True" 
                                Background="LightGray" 
                                FontFamily="Consolas" FontSize="16"/>
                        </DockPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>
        
        <TextBox 
            Text="{Binding Feedback}" 
            FontFamily="Consolas" FontSize="16"/>
    </DockPanel>
</UserControl>
